{
  "name": "blog-feed",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-feed.mjs",
    "serve": "python3 -m http.server 4000"
  },
  "dependencies": {
    "fast-xml-parser": "^5.8.0",
    "marked": "^13.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
